- name: Install utility packages
  ansible.builtin.apt:
    name: 
      - htop
      - iftop
      - rsync
      - screenfetch
      - vim
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
